#include <cstdio>
#include <cmath>

typedef unsigned long long ull;

const int MAX 1000000000000000ull;

ull val(ull S, ull x)
{
  return (x+1)*S + x*(x+1)/2;
}

ull solve(ull S)
{
  long double D = S*S + 3*S - 2
}

int main()
{
  ull S, x;
  scanf("%llu", &S);
  x = solve(S);
  ull val = (x+1)*S + x*(x+1)/2;
}
